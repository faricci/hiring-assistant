# Copilot instructions — Hiring Assistant

Read and follow `knowledge/ground-rules.md` before doing anything else. It is
the single, engine-agnostic source of truth for this repo's ground rules,
shared with Claude Code's `CLAUDE.md` — edit it there, not here, to change the
rules.

The specialized workflow lives in `.github/agents/hiring-assistant.agent.md`
and the phase files in `.github/instructions/`.
