# Claude Code instructions — Hiring Assistant

Read and follow `knowledge/ground-rules.md` before doing anything else. It is
the single, engine-agnostic source of truth for this repo's ground rules,
shared with GitHub Copilot's `.github/copilot-instructions.md` — edit it
there, not here, to change the rules.

The specialized workflow lives in the **Hiring Assistant** subagent
(`.claude/agents/hiring-assistant.md`) and the phase files in
`.github/instructions/`.
