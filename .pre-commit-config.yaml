repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest (hiring/ core test suite)
        entry: python -m pytest
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
