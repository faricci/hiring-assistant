"""Hiring Assistant — deterministic toolkit for a modular technical hiring process.

Public, team-agnostic. Team-specific content lives in ``teams/<id>/`` packs that
are selected via ``config/hiring.config.json`` (created by ``hiring setup``).
"""

__version__ = "1.0.0"
